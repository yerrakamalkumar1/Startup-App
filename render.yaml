services:
  - type: web
    name: connect-hub
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: HUGGINGFACE_API_KEY
        sync: false
