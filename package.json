{
  "name": "connect-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Connect Hub static app with a lightweight Node JSON backend.",
  "scripts": {
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
